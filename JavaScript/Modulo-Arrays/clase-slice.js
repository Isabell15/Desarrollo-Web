// slice()

const places = ['Park', 'Restaurant', 'Hospital', 'Cinema', 'House']

console.log(places.slice(2))
console.log(places.slice(2, 4))
console.log(places.slice(1, 6))
// conteo desde e final usando negative
console.log(places.slice(-3))
console.log(places.slice(1, -2))
console.log(places.slice())